import { ArrowUpRight, CircleCheck, Cloud, FolderKanban, ShieldCheck, Waypoints } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useAuth } from '../auth/useAuth'

export function OverviewPage() {
  const { user } = useAuth(); const firstName = user?.full_name?.split(' ')[0] || 'there'
  return <main className="overview"><section className="overview-hero"><div><p className="eyebrow"><span className="live-dot" />Workspace ready</p><h2>Good to see you, {firstName}.</h2><p>Your verification workspace is connected and ready for its first project.</p></div><div className="hero-gate" aria-hidden="true"><Waypoints /></div></section><section className="status-grid" aria-label="Workspace status"><article><div className="status-icon status-icon--green"><ShieldCheck /></div><div><p>Account status</p><strong>{user?.is_active ? 'Active' : 'Inactive'}</strong><small>Identity verified by API</small></div></article><article><div className="status-icon status-icon--cyan"><Cloud /></div><div><p>API connection</p><strong>Connected</strong><small>Session restored securely</small></div></article><article><div className="status-icon"><CircleCheck /></div><div><p>Foundation</p><strong>Phase 2 ready</strong><small>Project management configured</small></div></article></section><section className="empty-workspace"><div className="empty-visual" aria-hidden="true"><FolderKanban /><span /><span /></div><div><p className="eyebrow">Your verification record starts here</p><h3>Build your project workspace</h3><p>Projects connect repositories, test suites, runs, evidence, and issues into one traceable verification record.</p><Link className="workspace-action" to="/app/projects">Manage projects <ArrowUpRight /></Link><small>Create and maintain projects in Phase 2</small></div></section></main>
}
